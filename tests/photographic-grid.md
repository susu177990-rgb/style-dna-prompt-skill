# Test: photographic grid

Input: a 3x3 board of low-saturation live-action frames with window light, soft focus drift, off-center human subjects, and visible grain.

Expected:

- Select `photographic`.
- Prioritize lighting, lens/camera language, exposure, grain, skin/surface rendering.
- Do not output illustration-only fields such as brushwork or line quality.
- `execution_prompt.txt` contains concrete mechanisms, not just "cinematic".
