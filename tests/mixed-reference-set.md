# Test: mixed reference set

Input: a mixed board containing photography, 3D product renders, and poster layouts with only a shared pastel palette.

Expected:

- Select `mixed_uncertain`.
- Keep `locked_style` short.
- Mark low confidence and conflicting media signals in `quality_report.risks`.
- Do not over-lock camera, typography, or render-engine fields.
